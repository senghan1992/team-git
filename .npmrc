enable-pre-post-scripts=true
ignore-build-scripts=false
# 상위 디렉터리에 pnpm-workspace.yaml 이 있는 환경에서 pnpm 이 이 프로젝트를
# 그 워크스페이스의 (비)멤버로 취급해 `pnpm install` 이 아무것도 설치하지 않는
# 사고를 막는다 — 이 저장소는 항상 독립 프로젝트다.
ignore-workspace=true
